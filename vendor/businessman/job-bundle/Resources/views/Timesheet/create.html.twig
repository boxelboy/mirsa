{% extends '::app.html.twig' %}

{% block body %}
    <h1>
        Create timesheet
        <small>{{ job.description ? job.description : job.id }}</small>
    </h1>

    {{ form(form) }}
{% endblock %}
