A support ticket ({{ item.supportCall.id }}) has been updated by {{ user.staff.displayName }}. To view the full ticket, go to {{ url('support_calls_view', { supportCall: item.supportCall.id }) }}

--

{{ item.description }}