A support ticket ({{ item.supportCall.id }}) has been assigned to you by {{ user.staff.displayName }}. To view the full ticket, go to {{ url('support_calls_view', { supportCall: item.supportCall.id }) }}
{% if item.description %}

--

{{ item.description }}
{% endif %}